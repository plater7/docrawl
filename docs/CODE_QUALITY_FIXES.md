# Code Quality Fixes — generate_memory.py

Tracking file for medium/high severity issues found by automated code quality scan (2026-03-09).

## High Severity

### 1. Security: `tempfile.mktemp()` usage (DEPRECATED)
- **File:** `scripts/generate_memory.py`
- **Function:** `_compute_checksum()` (or wherever tempfile is used)
- **Issue:** `tempfile.mktemp()` is deprecated due to a TOCTOU race condition vulnerability. An attacker could predict or replace the temp file between name generation and file creation.
- **Fix:** Replace with `tempfile.NamedTemporaryFile()` or `tempfile.mkstemp()` which atomically create the file.
- [ ] Fixed

## Medium Severity

### 2. Silent exception swallowing (`except Exception: pass`)
- **File:** `scripts/generate_memory.py`
- **Functions:** `_build_module_map()`, `_list_workflows()`, `_list_adrs()`
- **Issue:** Multiple bare `except Exception: pass` blocks silently swallow all errors, making debugging impossible. If a file read fails, the error is completely hidden.
- **Fix:** At minimum, log the exception. Preferred: use specific exception types (e.g., `OSError`, `UnicodeDecodeError`) and log with `logging.warning()`.
- [ ] Fixed

---

*Auto-generated by Nebula code quality monitor. This PR will be updated with actual fixes.*